---
import Header from "../components/header.astro";
const { title } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <title>{title}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body>
  </body>
  <Header></Header>

    <main>
      <slot />
    </main>

    <footer>
      <p style=" color:#f5f7fa; ">📍 Tienda física · 📞 600 000 000</p>
    </footer>
  </body>
</html>

<style>
  :root {
    --primary: #1568a7;
    --bg: #f5f7fa;
    --dark: #1f2933;
    --light: #f5f7fa;
  }
  body {
    font-family: system-ui, sans-serif;
    margin: 0;
    background: var(--bg);
    color: var(--dark);
  }

  header {
    padding:1em;
    background: var(--primary);
    color: var(--light);
  }

  footer {
    background: #0f3558;
       color: var(--light);
  }

  main {
    padding: 2rem;
  }
</style>
